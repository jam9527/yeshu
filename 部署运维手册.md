# 椰树集团参观预约系统 — 部署运维手册

## 目录

1. [环境要求](#1-环境要求)
2. [部署步骤](#2-部署步骤)
3. [配置说明](#3-配置说明)
4. [小程序发布](#4-小程序发布)
5. [日常运维](#5-日常运维)
6. [常见问题](#6-常见问题)

---

## 1. 环境要求

### 服务器

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2 核 | 4 核 |
| 内存 | 4 GB | 8 GB |
| 磁盘 | 20 GB | 50 GB（SSD） |
| 操作系统 | Ubuntu 22.04 / CentOS 7+ | Ubuntu 22.04 |
| Node.js | >= 18 LTS | 20 LTS |
| MySQL | 8.0+ | 8.4 |
| Redis（可选） | 6.x+ | 7.x+ |

### 域名与证书

- 一个已备案的域名（用于小程序合法域名校验）
- HTTPS 证书（Let's Encrypt 或商业证书）
- 小程序管理后台已配置 `request` 合法域名

### 微信小程序

- 已注册的微信小程序 AppID
- 小程序管理后台开启：开发设置 → 服务器域名 → 添加 `https://你的域名`

---

## 2. 部署步骤

### 2.1 数据库部署

```bash
# 安装 MySQL 8.4
apt install mysql-server-8.4
mysql_secure_installation

# 创建数据库和用户
mysql -u root -p
CREATE DATABASE yeshu_reservation CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'yeshu'@'localhost' IDENTIFIED BY '你的密码';
GRANT ALL PRIVILEGES ON yeshu_reservation.* TO 'yeshu'@'localhost';
FLUSH PRIVILEGES;
```

### 2.2 后端部署

```bash
# 拉取代码
git clone <仓库地址> yeshu
cd yeshu/backend

# 安装依赖
npm install --production

# 配置环境变量
cp .env.example .env
# 编辑 .env 中的数据库连接、JWT Secret、微信配置等

# 编译
npm run build

# 使用 PM2 启动
npm install -g pm2
pm2 start dist/main.js --name yeshu-backend
pm2 save
pm2 startup

# 查看运行状态
pm2 status
pm2 logs yeshu-backend
```

### 2.3 管理后台部署

```bash
cd yeshu/admin-panel

# 安装依赖
npm install

# 构建
npm run build

# 配置 Nginx（将 dist 目录部署到 web 服务器）
# 或使用 PM2 静态服务:
npm install -g serve
serve -s dist -l 5173
```

### 2.4 Nginx 配置示例

```nginx
# /etc/nginx/sites-available/yeshu

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    # 管理后台
    location / {
        root /path/to/admin-panel/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 文件上传大小限制（申请表模板最大 10MB）
        client_max_body_size 10m;
    }

    # 静态资源（上传的图片、文件等）
    location /uploads/ {
        alias /path/to/backend/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

### 2.5 小程序编译与上传

```bash
cd mini-program
npm install
npm run build
```

使用微信开发者工具上传代码：
1. 打开 `mini-program` 目录
2. 点击「上传」按钮
3. 填写版本号和备注
4. 在小程序管理后台提交审核

---

## 3. 配置说明

### 3.1 后端环境变量 (.env)

| 变量 | 说明 | 示例 |
|------|------|------|
| `PORT` | 服务端口 | 3000 |
| `NODE_ENV` | 运行环境 | production |
| `DB_HOST` | 数据库地址 | localhost |
| `DB_PORT` | 数据库端口 | 3306 |
| `DB_USERNAME` | 数据库用户名 | yeshu |
| `DB_PASSWORD` | 数据库密码 |  |
| `DB_DATABASE` | 数据库名 | yeshu_reservation |
| `JWT_SECRET` | JWT 签名密钥 | 随机字符串（生产环境务必修改） |
| `JWT_EXPIRES_IN` | Token 有效期 | 7d |
| `WECHAT_APPID` | 小程序 AppID |  |
| `WECHAT_SECRET` | 小程序 Secret |  |
| `REDIS_HOST` | Redis 地址（可选） | localhost |
| `REDIS_PORT` | Redis 端口（可选） | 6379 |
| `TENCENT_SECRET_ID` | 腾讯云 SecretId |  |
| `TENCENT_SECRET_KEY` | 腾讯云 SecretKey |  |
| `TENCENT_REGION` | 腾讯云地域 | ap-guangzhou |
| `COS_BUCKET` | COS 存储桶名称 |  |
| `THROTTLE_TTL` | 限流时间窗口（毫秒） | 60000 |
| `THROTTLE_LIMIT` | 限流次数 | 100 |

### 3.2 管理后台环境变量

在 `admin-panel/` 目录创建 `.env` 文件：

```env
VITE_API_BASE_URL=https://your-domain.com/api
```

### 3.3 小程序配置

`mini-program/config.ts` 中的 `API_BASE_URL` 需指向后端地址：

```typescript
// 生产环境使用 HTTPS 域名
exports.API_BASE_URL = 'https://your-domain.com/api';

// 开发环境使用局域网 IP（手机预览）
// exports.API_BASE_URL = 'http://192.168.1.xxx:3000/api';
```

---

## 4. 小程序发布

### 4.1 提交流程

1. **编译**：`cd mini-program && npm run build`
2. **打开开发者工具**，选择「上传」
3. **填写版本号**：遵循语义化版本（如 `1.0.0`）
4. **填写更新说明**：描述本次变更
5. **提交审核**：在小程序管理后台 → 版本管理 → 提交审核
6. **审核通过后发布**：点击「发布」

### 4.2 发布前检查清单

- [ ] 后端已部署并正常运行
- [ ] `config.ts` 中 `API_BASE_URL` 指向生产环境
- [ ] HTTPS 证书有效
- [ ] 小程序管理后台已配置 request 合法域名
- [ ] 管理员账号已初始化（通过 MySQL 直接插入或注册接口）
- [ ] 首页 DIY 配置已发布
- [ ] 可预约日期已配置（或等待次日凌晨自动创建）

---

## 5. 日常运维

### 5.1 后端管理

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs yeshu-backend

# 重启
pm2 restart yeshu-backend

# 重新加载（零停机）
pm2 reload yeshu-backend
```

### 5.2 定时任务说明

系统内置以下定时任务（每天自动执行）：

| 时间 | 任务 | 说明 |
|------|------|------|
| 01:00 | 滚动创建预约日期 | 自动创建未来 30 天的日期配置和配额（跳过周一） |
| 02:00 | 处理过期预约 | 将过期未核销的预约标记为 `EXPIRED`，递增用户过期次数 |
| 02:30 | 自动拉黑 | 过期次数 >= 3 的用户自动拉黑 90 天；移除已到期的黑名单 |

### 5.3 数据库备份

```bash
# 每日备份
mysqldump -u yeshu -p yeshu_reservation > backup_$(date +%Y%m%d).sql

# 保留最近 30 天备份，删除更早的
find /backup/mysql -name "*.sql" -mtime +30 -delete
```

推荐使用 crontab 自动备份：

```cron
0 3 * * * /usr/bin/mysqldump -u yeshu -p'密码' yeshu_reservation > /backup/mysql/backup_$(date +\%Y\%m\%d).sql 2>/dev/null && find /backup/mysql -name "*.sql" -mtime +30 -delete
```

### 5.4 日志监控

后端日志可通过 PM2 查看。如需持久化日志：

```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

### 5.5 管理员账号初始化

首次使用需要通过命令行创建管理员账号：

```bash
# 方式一：直接插入数据库
mysql -u root -p yeshu_reservation
INSERT INTO admin_users (username, password, role_id, status) VALUES
('admin', '$2a$...bcrypt_hash...', 1, 1);
# 密码需使用 bcrypt 加密

# 方式二：通过注册接口（如已开放）
curl -X POST https://your-domain.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"初始密码"}'
```

---

## 6. 常见问题

### 6.1 小程序白屏 / 加载失败

**原因：** API 地址配置错误或后端未启动。

**排查：**
1. 打开小程序调试模式（右上角 → 开发调试）
2. 查看 Network 面板，确认请求是否发出
3. 确认 `config.ts` 中 `API_BASE_URL` 可访问
4. 确认微信管理后台已配置 request 合法域名

### 6.2 `routeDone with a webviewId X is not found`

**原因：** 页面 JS 文件包含 `exports.__esModule`，在 `enhance: false` 模式下 `exports` 未定义。

**解决：**
```bash
cd mini-program
npm run build
```

然后清除微信开发者工具缓存（工具栏 → 清除 → 全部清除），重启项目。

### 6.3 WXML 编译报错 `unexpected token '.'`

**原因：** WXML 数据绑定不支持可选链（`?.`）和空值合并（`??`）语法。

**解决：** 使用 `&&` 和 `||` 替代：
```html
<!-- 错误 -->
<text>{{item.props.style?.fontSize || '14px'}}</text>
<!-- 正确 -->
<text>{{(item.props.style && item.props.style.fontSize) || '14px'}}</text>
```

### 6.4 管理后台登录提示"登录已过期"

**原因：** Token 过期或服务器时间不同步。

**解决：** 清除浏览器 localStorage 中的 `admin_token`，重新登录。

### 6.5 预约名额不足（乐观锁冲突）

**原因：** 高并发下多个用户同时预约，配额扣减冲突。

**说明：** 系统使用 MySQL 乐观锁（`version` 字段）防止超售，冲突时返回错误提示。用户刷新后重新尝试即可。这是正常行为，保证了数据一致性。

### 6.6 实名认证失败

**排查：**
1. 确认腾讯云 SecretId/SecretKey 配置正确
2. 确认 FaceID 服务已开通
3. 确认用户姓名和身份证号一致
4. 查看后端日志确认具体错误原因

### 6.7 小程序审核被拒

**常见原因：**
- 首页无实际内容或仅显示"请登录管理后台配置" → 确保每次提交前首页 DIY 已配置并发布
- 隐私政策缺失 → 在 `app.json` 中配置 `requiredPrivateInfos` 并在页面展示隐私协议
- 测试账号不可用 → 提供测试账号或确保无登录强制页面可正常访问
